.DS_Store
.claude

# Jekyll build artifacts
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/
/vendor/          # Bundler's local gem dir (root only) — NOT assets/vendor/ (self-hosted CSS/fonts)
.bundle/
